

import { valida } from `./validate.js`

const inputs = document.querySelectorAll("inpput")

inputs.forEach(input => {
    input.addEventListener("valida(input:any"); 
        valida(evento.target)
})